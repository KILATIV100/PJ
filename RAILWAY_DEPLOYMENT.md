# 🚀 РОЗГОРТАННЯ НА RAILWAY

**Для: Backend + MongoDB на Railway**

---

## ✅ ЩО ВЖЕ ГОТОВО

- ✅ MongoDB на Railway налаштована
- ✅ Backend код готовий
- ✅ .env з connection string
- ✅ Git репозиторій готовий

---

## 🎯 ПЛАН РОЗГОРТАННЯ

```
┌─────────────────┐
│  Ваш Git Repo   │
│  (GitHub/etc)   │
└────────┬────────┘
         │
         ▼
┌──────────────────────────┐
│  Railway Dashboard       │
│  (Приєднати репозиторій) │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│  Backend на Railway      │
│  - Node.js + Express     │
│  - Автоматичний деплой   │
└──────────────────────────┘
         │
         ▼
┌──────────────────────────┐
│  MongoDB на Railway      │
│  - Вже налаштована       │
│  - Connection string OK  │
└──────────────────────────┘
```

---

## 📋 КРОК 1:準готуйте Repository на GitHub

### 1.1 Переконайтесь, що у вас є GitHub акаунт
- Перейдіть https://github.com
- Зареєструйтесь (якщо потрібно)

### 1.2 Створіть новий репозиторій на GitHub
```bash
# Відкрийте https://github.com/new
# Заповніть:
Repository name: pro-jet
Description: Laser cutting platform
Visibility: Public або Private (на ваш вибір)
```

### 1.3 Завантажте код на GitHub

```bash
# У вашій папці проекту:
git remote add origin https://github.com/YOUR_USERNAME/pro-jet.git
git branch -M main
git push -u origin main
```

---

## 🚄 КРОК 2: Railway Development Environment

### 2.1 Зареєструйтесь на Railway
```
1. Перейдіть https://railway.app
2. Натисніть "Start building"
3. Виберіть "Deploy from GitHub"
4. Дозвольте Railway доступ до вашого GitHub
```

### 2.2 Приєднайте ваш репозиторій
```
1. У Railway Dashboard натисніть "New Project"
2. Виберіть "Deploy from GitHub repo"
3. Виберіть ваш "pro-jet" репозиторій
4. Натисніть "Deploy"
```

Railway автоматично:
- ✅ Встановить залежності (`npm install`)
- ✅ Запустить build
- ✅ Розгорне на своїх серверах

### 2.3 Додайте MongoDB (якщо ще не додана)
```
1. У Railway проекту натисніть "New Service"
2. Виберіть "Database" → "MongoDB"
3. Натисніть "Deploy"
```

---

## 🔐 КРОК 3: Налаштування змінних середовища

### 3.1 Додайте змінні в Railway Dashboard
```
1. Перейдіть до вашого Railway проекту
2. Натисніть на "Variables"
3. Додайте всі змінні з .env файлу:
```

| Ключ | Значення |
|------|----------|
| `NODE_ENV` | `production` |
| `PORT` | `3000` |
| `MONGODB_URI` | `mongodb://mongo:PASSWORD@mongodb.railway.internal:27017/pro-jet` |
| `JWT_SECRET` | `generate_strong_secret_key_here` |
| `ADMIN_EMAIL` | `admin@pro-jet.ua` |
| `ADMIN_PASSWORD` | `change_to_strong_password` |

### 3.2 Отримайте MONGODB_URI з Railway MongoDB
```
1. У вашому Railway проекті натисніть на MongoDB сервіс
2. Натисніть "Variables"
3. Скопіюйте "DATABASE_URL" або "MONGODB_URI"
4. Вставте його у змінні вашого Backend
```

---

## 🌐 КРОК 4: Отримайте URL вашого Backend

### 4.1 Знайдіть Deploy URL
```
1. У Railway Dashboard натисніть на Backend (Node.js) сервіс
2. Натисніть "Deployments"
3. Скопіюйте URL (щось на кшталт):
   https://pro-jet-production.up.railway.app
```

### 4.2 Перевірте, що сервер запущено
```bash
curl https://pro-jet-production.up.railway.app/api/health

# Ви повинні отримати:
# {"status":"OK","timestamp":"2024-11-26T..."}
```

---

## 📱 КРОК 5: Оновіть Frontend API URLs

Тепер потребує оновити URL у frontend файлах щоб вони вказували на Railway:

### 5.1 Оновіть checkout.html
```bash
# Откройте checkout.html
# Знайдіть рядок ~287:
const API_URL = 'http://localhost:3000/api';

# Замініть на:
const API_URL = 'https://pro-jet-production.up.railway.app/api';
```

### 5.2 Оновіть auth.html
```bash
# Откройте auth.html
# Знайдіть рядок ~216:
const API_URL = 'http://localhost:3000/api';

# Замініть на:
const API_URL = 'https://pro-jet-production.up.railway.app/api';
```

### 5.3 Оновіть profile.html
```bash
# Откройте profile.html
# Знайдіть рядок ~333:
const API_URL = 'http://localhost:3000/api';

# Замініть на:
const API_URL = 'https://pro-jet-production.up.railway.app/api';
```

### 5.4 Оновіть shop.js
```bash
# Откройте shop.js (якщо там є API_URL)
# Замініть усі localhost на Railway URL
```

---

## 🧪 КРОК 6: Тестування

### 6.1 Перевірте, що backend запущено
```bash
curl https://your-railway-url.up.railway.app/api/health
```

### 6.2 Тестуйте реєстрацію
```bash
curl -X POST https://your-railway-url.up.railway.app/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Test",
    "lastName": "User",
    "email": "test@example.com",
    "password": "Test123456",
    "passwordConfirm": "Test123456"
  }'
```

### 6.3 Тестуйте замовлення
```bash
curl -X POST https://your-railway-url.up.railway.app/api/orders \
  -H "Content-Type: application/json" \
  -d '{
    "customer": {
      "name": "Test User",
      "email": "test@example.com",
      "phone": "+380501234567",
      "city": "Київ"
    },
    "service": "shop",
    "pricing": {
      "basePrice": 500,
      "discount": 0,
      "totalPrice": 500,
      "currency": "UAH"
    },
    "payment": {
      "method": "fondy",
      "status": "pending"
    }
  }'
```

---

## 📱 КРОК 7: Розгорніть Frontend на Netlify (Опціонально)

### 7.1 Завантажте на Netlify
```bash
# Простий спосіб - Drag & Drop
1. Перейдіть https://app.netlify.com
2. Drag & drop вашу папку проекту (без node_modules та .env)
3. Натисніть "Deploy"

# Або через Git
1. Додайте репозиторій на GitHub
2. Пов'яжіть Netlify з GitHub
3. Netlify автоматично розгорне при push
```

### 7.2 Отримайте Netlify URL
```
https://your-project.netlify.app
```

---

## ✅ ПЕРЕВІРОЧНИЙ СПИСОК

Перед розгортанням переконайтесь:

- ✅ GitHub репозиторій створений та код завантажений
- ✅ Railway акаунт створений
- ✅ Railway проект приєднаний до GitHub
- ✅ MongoDB додана як сервіс в Railway
- ✅ Змінні середовища додані в Railway
- ✅ Frontend API URLs оновлені на Railway URLs
- ✅ Backend запущений на Railway
- ✅ MongoDB підключена до Backend
- ✅ API endpoints тестовані (curl)
- ✅ Frontend розгорнутий (опціонально)

---

## 🔗 ВАЖЛИВІ URLs

Після розгортання у вас буде:

| Компонент | URL |
|-----------|-----|
| Backend API | `https://pro-jet-production.up.railway.app` |
| Health Check | `https://pro-jet-production.up.railway.app/api/health` |
| MongoDB | Внутрішня (з Railway Docker мережі) |
| Frontend | `https://your-project.netlify.app` (якщо на Netlify) |
| Admin Login | `https://your-project.netlify.app/admin-login` |
| Shop | `https://your-project.netlify.app/shop` |

---

## 🛠️ Якщо щось не працює

### Помилка: "Build failed on Railway"

**Рішення:**
```bash
1. Перевірте логи в Railway Dashboard
2. Переконайтесь, що у проекту є package.json
3. Переконайтесь, що start script визначена
4. Спробуйте `npm install` локально перед push
```

### Помилка: "MongoDB connection failed"

**Рішення:**
```
1. Перевірте, що MongoDB додана як сервіс
2. Переконайтесь, що CONNECTION STRING правильна
3. Переконайтесь, що MONGODB_URI змінна встановлена
4. Спробуйте перезапустити сервіс
```

### Помилка: "CORS error" на Frontend

**Рішення:**
```
1. Перевірте, що CORS налаштований у server.js
2. Переконайтесь, що frontend URL додано як origin
3. Спробуйте origin: '*' для тестування
```

### Помилка: API повертає 404

**Рішення:**
```
1. Перевірте, що routes правильно імпортовані в server.js
2. Перевірте, що API URL у frontend правильна
3. Перевірте логи Railway backend
```

---

## 📊 ПОРІВНЯННЯ ДО/ПІСЛЯ

### До (Локально)
```
Frontend:   http://localhost:3000
Backend:    http://localhost:3000
MongoDB:    localhost:27017
```

### Після (Railway + Netlify)
```
Frontend:   https://your-project.netlify.app
Backend:    https://pro-jet.up.railway.app
MongoDB:    Railway Cloud
```

---

## 📝 ДЕПЛОЙ ПРОЦЕС SUMMARY

```
1. GitHub repo готовий        [5 хвилин]
2. Railway проект створений   [2 хвилини]
3. MongoDB додана              [1 хвилина]
4. Змінні налаштовані         [2 хвилини]
5. Frontend URLs оновлені     [5 хвилин]
6. Deploy завершений          [Автоматично]
7. Тестування                 [10 хвилин]

ИТОГО: 25-30 хвилин ⏱️
```

---

## 🎯 НАСТУПНІ КРОКИ

**Якщо все працює:**
1. ✅ Backend на Railway
2. ✅ MongoDB на Railway
3. ✅ Frontend на Netlify (опціонально)
4. ✅ Система повністю live!

**Що робити дальше:**
- Тестуйте всі функції
- Додайте платіжні ключі (Fondy, LiqPay)
- Налаштуйте email (SMTP)
- Додайте Nova Poshta API ключ

---

**Готові! Давайте розгорнемо! 🚀**
