# ⚡ ШВИДКИЙ СТАРТ: Локальне тестування (10 хвилин)

Найшвидший спосіб отримати робочу форму замовлень локально.

---

## 🎯 ВЫБЕРИТЕ ВАШИЙ ОС

### 🍎 macOS

```bash
# КРОК 1: Встановіть MongoDB (якщо не встановлена)
brew tap mongodb/brew
brew install mongodb-community

# КРОК 2: Запустіть MongoDB
brew services start mongodb-community

# КРОК 3: Перейдіть в папку проекту
cd /home/user/PJ

# КРОК 4: Встановіть залежності
npm install

# КРОК 5: Запустіть сервер
npm start

# КРОК 6: Відкрийте в браузері
# http://localhost:3000/checkout
```

### 🐧 Linux (Ubuntu/Debian)

```bash
# КРОК 1: Встановіть MongoDB
sudo apt-get update
sudo apt-get install -y mongodb

# КРОК 2: Запустіть MongoDB
sudo systemctl start mongodb

# КРОК 3: Перейдіть в папку проекту
cd /home/user/PJ

# КРОК 4: Встановіть залежності
npm install

# КРОК 5: Запустіть сервер
npm start

# КРОК 6: Відкрийте в браузері
# http://localhost:3000/checkout
```

### 🪟 Windows (рекомендується Docker)

**Варіант A: Docker (найпростіший)**

```bash
# КРОК 1: Запустіть MongoDB у Docker
docker run -d --name mongodb -p 27017:27017 mongo:latest

# КРОК 2: Перейдіть в папку проекту
cd C:\path\to\PJ
# ИЛИ якщо через WSL:
cd /mnt/c/path/to/PJ

# КРОК 3: Встановіть залежності
npm install

# КРОК 4: Запустіть сервер
npm start

# КРОК 5: Відкрийте в браузері
# http://localhost:3000/checkout
```

**Варіант B: MongoDB Community**

1. Завантажте: https://www.mongodb.com/try/download/community
2. Встановіть (вибрати "Install MongoDB as a Service")
3. Послуга автоматично запуститься
4. Виконайте кроки 3-5 звичайно

---

## 🔧 НАЛАШТУВАННЯ .env

Відкрийте файл `.env` та замініть строку:

**З:**
```env
MONGODB_URI=mongodb://mongo:lXnfIbBpaaIcYXjDMQDZxEEVEitglQwu@mongodb.railway.internal:27017/pro-jet
```

**На:**
```env
MONGODB_URI=mongodb://localhost:27017/pro-jet
```

Все! Інші конфігурації залишите як є.

---

## ✅ ПЕРЕВІРКА

### 1. MongoDB запущена?

```bash
# Повинна бути команда у mongosh
mongosh
# Натисніть Ctrl+C для виходу
```

Якщо помилка "mongosh: command not found", MongoDB не встановлена.

### 2. Сервер запущено?

Дивіться терміналу, повинна бути:

```
╔════════════════════════════════════════╗
║     🚀 Pro Jet Backend Server 🚀       ║
╚════════════════════════════════════════╝

  URL: http://localhost:3000

✅ MongoDB підключена успішно!
```

Якщо ошибка ECONNREFUSED:
- MongoDB не запущена
- Перезапустіть MongoDB

### 3. Форма доступна?

Відкрийте: http://localhost:3000/checkout

Повинна бути форма.

---

## 📝 ТЕСТУВАННЯ ФОРМИ

### 1. Додайте товар

1. Натисніть на "Магазин" у меню
2. Натисніть на будь-який товар
3. Натисніть "Додати у кошик"
4. Натисніть "Оформити"

### 2. Заповніть форму

```
Ім'я: Тест
Прізвище: Користувач
Email: test@example.com
Телефон: +380501234567
Місто: Київ
Адреса: вул. Тестова, 1
Коментарі: Тестове замовлення
```

### 3. Натисніть "Відправити"

Повинне з'явитися:
```
✅ Замовлення успішно створено!
```

### 4. Перевірте БД

**Через mongosh:**

```bash
mongosh
use pro-jet
db.orders.find()
# Повинно бути ваше замовлення в JSON
exit
```

---

## 🎯 ПЕРЕВІРКА API

```bash
# Тестовий запит
curl http://localhost:3000/api/health

# Повинна бути відповідь:
# {"status":"OK","message":"Pro Jet Backend API is running"}
```

---

## 🆘 ЧАСТЫЕ ПОМИЛКИ

### ❌ Помилка: "ECONNREFUSED"

```
Error: connect ECONNREFUSED ::1:27017
```

**Рішення:**
```bash
# MongoDB не запущена, запустіть:

# macOS
brew services start mongodb-community

# Linux
sudo systemctl start mongodb

# Docker
docker start mongodb
```

### ❌ Помилка: "Cannot find module 'mongoose'"

```
Error: Cannot find module 'mongoose'
```

**Рішення:**
```bash
npm install
```

### ❌ Форма не відправляється

**Перевірте в DevTools (F12):**

1. Натисніть F12
2. Перейдіть у вкладку "Console"
3. Заповніть форму та натисніть "Відправити"
4. Дивіться помилку

Типові помилки:
- `POST /api/orders 500` - MongoDB проблема
- `POST /api/orders 400` - Неправильні дані у формі

### ❌ MongoDB не знаходиться

```
mongosh: command not found
```

**Рішення:**

- Переінсталюйте MongoDB
- Або використовуйте Docker (простіше)

---

## 🚀 НАСТУПНІ КРОКИ

Коли форма працює локально:

1. ✅ Налаштуйте Telegram Bot (дивіться TELEGRAM_SETUP.md)
2. ✅ Розгорніть на Railway (дивіться RAILWAY_INSTRUCTIONS.txt)
3. ✅ Тестуйте на живому сайті

---

## 📝 Контроль список

- [ ] MongoDB встановлена та запущена
- [ ] `.env` оновлена (MONGODB_URI=mongodb://localhost...)
- [ ] `npm install` виконана
- [ ] `npm start` працює без ошибок
- [ ] http://localhost:3000/checkout доступна
- [ ] Форма заповняється та відправляється
- [ ] Замовлення з'являється у MongoDB

---

**Готово! Теперь у вас є робочий сервер із формою замовлень!** 🎉

---

**Остання оновлення:** 02.12.2024
