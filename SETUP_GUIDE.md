# Pro Jet - Керівництво з Налаштування

## 📋 Зміст
1. [Базова конфігурація](#базова-конфігурація)
2. [MongoDB на Railway](#mongodb-на-railway)
3. [Розгортання на Railway](#розгортання-на-railway)
4. [Розгортання Фронтенду](#розгортання-фронтенду)
5. [Перевірка функціональності](#перевірка-функціональності)
6. [Поточний статус проекту](#поточний-статус-проекту)

---

## Базова конфігурація

### 1. Встановіть залежності локально

```bash
npm install
```

### 2. Створіть файл `.env`

```bash
cp .env.example .env
```

Або вручну створіть файл `.env` з таким вмістом:

```env
# Сервер
NODE_ENV=development
PORT=3000
HOST=localhost

# MongoDB
MONGODB_URI=mongodb://localhost:27017/pro-jet

# JWT
JWT_SECRET=your_super_secret_key_here_change_in_production

# Нова Пошта API
NOVA_POSHTA_API_KEY=your_api_key_here

# Email (Nodemailer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_password

# Платіжні системи
FONDY_API_KEY=your_fondy_key
LIQPAY_PUBLIC_KEY=your_liqpay_key
LIQPAY_PRIVATE_KEY=your_liqpay_secret

# Адміністратор
ADMIN_EMAIL=admin@pro-jet.ua
ADMIN_PASSWORD=admin123456
```

---

## MongoDB на Railway

### Крок 1: Створіть акаунт на Railway

1. Перейдіть на https://railway.app
2. Натисніть "Sign Up" і зареєструйтесь через GitHub

### Крок 2: Створіть MongoDB проект

```bash
# Після входу в Railway dashboard:
1. Натисніть "New Project"
2. Виберіть "Database"
3. Виберіть "MongoDB"
4. Натисніть "Deploy"
```

### Крок 3: Отримайте Connection String

```bash
1. Перейдіть в проект MongoDB
2. Натисніть на "Variables"
3. Скопіюйте `DATABASE_URL` - це ваша connection string
4. Формат: mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
```

### Крок 4: Оновіть `.env` файл локально

```env
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pro-jet?retryWrites=true&w=majority
```

### Крок 5: Перевірте підключення

```bash
# Запустіть сервер локально
npm start

# Або для development з автоперезавантаженням
npm run dev
```

Ви повинні бачити в консолі:
```
✅ MongoDB підключена успішно!
```

---

## Розгортання на Railway

### Крок 1: Підготовка Repository

```bash
# Переконайтесь, що все закомітено
git status

# Якщо є зміни:
git add .
git commit -m "Deploy to Railway"
```

### Крок 2: Розгортання

```bash
# Встановіть Railway CLI (якщо ще не встановлено)
npm install -g @railway/cli

# Залогінься
railway login

# Ініціалізуйте Railway проект
railway init

# Розгорніть
railway up
```

### Крок 3: Налаштування середовища на Railway

```bash
# В Railway Dashboard:
1. Перейдіть до вашого проекту
2. Натисніть "Variables"
3. Додайте всі змінні з файлу .env

# Обов'язкові змінні:
NODE_ENV=production
MONGODB_URI=[скопіюйте з MongoDB проекту]
JWT_SECRET=[придумайте складний пароль]
PORT=3000
```

### Крок 4: Отримайте URL

```bash
# В Railway Dashboard, натисніть "Deployments"
# Скопіюйте URL вашого сервера - щось на кшталт:
# https://your-project-production.railway.app
```

---

## Розгортання Фронтенду

### Опція 1: Netlify

```bash
# 1. Перейдіть на https://app.netlify.com
# 2. Натисніть "Add new site"
# 3. Виберіть "Deploy manually"
# 4. Drag & drop папку вашого проекту (тільки HTML/CSS/JS файли)

# Або скористайтесь Netlify CLI:
npm install -g netlify-cli
netlify deploy --prod --dir=.
```

### Опція 2: Vercel

```bash
# 1. Перейдіть на https://vercel.com
# 2. Натисніть "New Project"
# 3. Імпортуйте ваш Git репозиторій
# 4. Розгорніть
```

### Обновіть API URL в файлах

Якщо ваш backend розгорнутий на Railway, оновіть URL:

```javascript
// В checkout.html, shop.js, auth.html, profile.html
const API_URL = 'https://your-railway-url.railway.app/api';
```

---

## Перевірка функціональності

### 1. Тест Магазину (Shop)

```
1. Перейдіть на http://localhost:3000/shop
2. Додайте товари в кошик
3. Натисніть "Оформити замовлення"
4. Заповніть форму checkout
5. Натисніть "Оформити Замовлення"
```

### 2. Тест Реєстрації

```
1. Перейдіть на http://localhost:3000/auth
2. Виберіть "Реєстрація"
3. Введіть:
   - Ім'я: Тест
   - Прізвище: Юзер
   - Email: test@example.com
   - Пароль: Test12345
4. Натисніть "Зареєструватися"
```

### 3. Тест Адмін Панелі

```
1. Перейдіть на http://localhost:3000/admin-login
2. Демо облікові дані:
   - Email: admin@pro-jet.ua
   - Пароль: admin123456
3. Натисніть "Увійти"
4. Ви повинні потрапити на http://localhost:3000/admin
5. Там ви можете переглядати:
   - Замовлення
   - Користувачів
   - Товари
   - Статистику
```

### 4. Перевірте API endpoints

```bash
# Створіть замовлення
curl -X POST http://localhost:3000/api/orders \
  -H "Content-Type: application/json" \
  -d '{
    "customer": {"name": "Іван", "email": "ivan@example.com", "phone": "+380501234567"},
    "service": "shop",
    "pricing": {"basePrice": 500, "discount": 0, "totalPrice": 500, "currency": "UAH"},
    "payment": {"method": "fondy", "status": "pending"}
  }'

# Отримайте всі замовлення
curl http://localhost:3000/api/admin/orders

# Зареєструйте користувача
curl -X POST http://localhost:3000/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Іван",
    "lastName": "Петренко",
    "email": "ivan@example.com",
    "password": "password123",
    "passwordConfirm": "password123"
  }'
```

---

## Поточний статус проекту

### ✅ Реалізовано

- **Frontend**
  - ✅ Responsивний дизайн (mobile, tablet, desktop)
  - ✅ Магазин із товарами і кошиком
  - ✅ Галерея проектів
  - ✅ Калькулятори (гравіювання, різання)
  - ✅ Checkout з guest/login/register опціями
  - ✅ Сторінка профілю користувача
  - ✅ Admin-login сторінка

- **Backend**
  - ✅ Express.js сервер
  - ✅ MongoDB интеграція
  - ✅ REST API для замовлень
  - ✅ User authentication (JWT + bcrypt)
  - ✅ Admin панель API
  - ✅ Nova Poshta API интеграція
  - ✅ Payment гейтвеї (Fondy, LiqPay)

### 🔄 У розробці / Потребує доопрацювання

- 🔄 Email verification на реєстрацію
- 🔄 Password reset функціональність
- 🔄 Payment callback обробка
- 🔄 Order tracking від Nova Poshta
- 🔄 Seller marketplace функціональність
- 🔄 SEO оптимізація
- 🔄 Аналітика Google

### 🚀 Готово до запуску

Проект готовий до розгортання на Railway + Netlify:

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   Netlify   │       │   Railway   │       │   MongoDB   │
│  (Frontend) │◄─────►│  (Backend)  │◄─────►│   (Atlas)   │
│ HTML/CSS/JS │       │ Express.js  │       │  Database   │
└─────────────┘       └─────────────┘       └─────────────┘
```

---

## Утилітарні команди

```bash
# Запустити локально
npm start

# Запустити з автоперезавантаженням (потребує nodemon)
npm run dev

# Перевірити MongoDB
mongosh mongodb+srv://username:password@cluster.mongodb.net/pro-jet

# Очистити кеш npm
npm cache clean --force

# Переустановити залежності
rm -rf node_modules
npm install
```

---

## Поширені проблеми та рішення

### Проблема: "Cannot find module 'mongoose'"

```bash
# Рішення:
npm install mongoose
```

### Проблема: "MongooseError: Cannot connect to MongoDB"

```bash
# Перевірте:
1. Правильність MONGODB_URI в .env
2. IP адресу в MongoDB Atlas whitelist (дозволити 0.0.0.0/0)
3. Інтернет з'єднання
```

### Проблема: "CORS error" при checkout

```bash
# Перевірте в server.js:
app.use(cors({
  origin: '*',  // Дозволяє всім фронтенд адресам
  credentials: true,
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS']
}));
```

### Проблема: Checkout не працює

```
1. Перевірте, що сервер запущено (http://localhost:3000)
2. Відкрийте DevTools (F12) > Console
3. Перевірте помилки
4. Переконайтесь, що MONGODB_URI правильна в .env
```

---

## Контакти для підтримки

Якщо виникають проблеми:
1. Перевірте консоль браузера (F12 > Console)
2. Перевірте терміналь сервера на помилки
3. Перевірте MongoDB статус на Railway
4. Прочитайте README.md для більше деталей

---

**Остання оновлення:** 26.11.2024
**Версія:** 1.0.0
**Статус:** Production Ready 🚀
