# 🗄️ Інструкція: Підключення БД та тестування форми

Цей гайд показує як правильно підключити MongoDB та отримати робочу форму замовлень.

---

## 🎯 ШВИДКИЙ СТАРТ (5 хвилин)

### ВАРІАНТ 1: Тестування на Railway (рекомендується)

```bash
# 1. Push код на GitHub
git push origin main

# 2. Розгорніть на Railway (автоматично)

# 3. Додайте Telegram Bot
# Дивіться TELEGRAM_SETUP.md

# 4. Тестуйте на живому сайті!
```

### ВАРІАНТ 2: Локальне тестування з MongoDB

```bash
# 1. Встановіть MongoDB локально (дивіться нижче)

# 2. Запустіть MongoDB

# 3. npm install && npm start

# 4. Відкрийте http://localhost:3000/checkout

# 5. Заповніть форму та отправте!
```

---

## 📋 ДЛЯ РОЗУМІННЯ: Як це працює

```
┌─────────────────────┐
│   ФОРМА ЗАМОВЛЕННЯ   │ checkout.html
│  (browser)          │
└──────────┬──────────┘
           │
           │ POST /api/orders
           │ JSON: {customer, items, pricing, notes}
           │
           ↓
┌─────────────────────┐
│  EXPRESS SERVER     │ server.js:3000
│  routes/orders.js   │
└──────────┬──────────┘
           │
           ├─→ Валідація даних
           ├─→ Зберегти в MongoDB
           ├─→ Відправити у Telegram
           └─→ Відповідь: {success, order}

           ↓
┌─────────────────────┐
│  MONGODB           │
│  /pro-jet          │ (база даних)
│  /orders           │ (колекція)
└─────────────────────┘
```

---

## 🔧 КРОК 1: Налаштування MongoDB

### ВАРІАНТ A: MongoDB Atlas (хмарний - найпростіший)

**1. Зареєструйтесь**

Перейдіть на: https://www.mongodb.com/cloud/atlas

- Sign Up з email
- Підтвердіть email
- Створіть Organization

**2. Створіть Project**

- Click "Create a Project"
- Назва: "pro-jet"
- Click "Create Project"

**3. Створіть Cluster**

- Click "Create a Cluster"
- Виберіть "Shared" (вільна)
- Cloud Provider: AWS
- Region: eu-west-1 (Ірландія - близько до України)
- Cluster Name: "pro-jet-cluster"
- Click "Create Cluster"

**4. Створіть Database User**

- Click "Database Access"
- Click "Add New Database User"
- Username: `mongo`
- Password: Генеруйте (або введіть: `YourStrongPassword123`)
- Click "Add User"

**5. Дозвольте IP Address**

- Click "Network Access"
- Click "Add IP Address"
- Виберіть "Allow access from anywhere" (для тестування)
- Click "Confirm"

**6. Отримайте Connection String**

- Click "Databases"
- Click "Connect" на вашому кластері
- Виберіть "Drivers"
- Виберіть "Node.js" та версію
- Скопіюйте строку типу:

```
mongodb+srv://mongo:<password>@pro-jet-cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
```

**7. Оновіть .env**

Замініть `mongodb.railway.internal` на вашу Atlas строку:

```env
MONGODB_URI=mongodb+srv://mongo:YourPassword@pro-jet-cluster.xxxxx.mongodb.net/pro-jet?retryWrites=true&w=majority
```

---

### ВАРІАНТ B: MongoDB Community (локально)

**Для Linux/Mac:**

```bash
# macOS (з Homebrew)
brew tap mongodb/brew
brew install mongodb-community
brew services start mongodb-community

# Linux (Ubuntu)
sudo apt-get install -y mongodb
sudo systemctl start mongodb

# Windows
# Завантажте: https://www.mongodb.com/try/download/community
# Встановіть installer
# Запустіть MongoDB
```

**Перевірте установку:**

```bash
mongosh
# Повинна бути командна строка MongoDB
exit
```

**Оновіть .env для локального використання:**

```env
MONGODB_URI=mongodb://localhost:27017/pro-jet
```

---

### ВАРІАНТ C: Docker (якщо встановлений)

```bash
# Запустіть MongoDB у Docker
docker run -d \
  --name mongodb \
  -p 27017:27017 \
  -e MONGO_INITDB_ROOT_USERNAME=mongo \
  -e MONGO_INITDB_ROOT_PASSWORD=password \
  mongo:latest

# Оновіть .env
MONGODB_URI=mongodb://mongo:password@localhost:27017/pro-jet
```

---

## 🚀 КРОК 2: Налаштування коду

### 1. Встановіть залежності

```bash
cd /home/user/PJ
npm install
```

Повинні встановитися:
- ✅ express
- ✅ mongoose
- ✅ node-telegram-bot-api
- ✅ і інші

### 2. Налаштуйте .env

Відкрийте `.env` та оновіть:

```env
# Обов'язково змініть

# Database
MONGODB_URI=mongodb+srv://mongo:YourPassword@pro-jet-cluster...
# ИЛИ для локального
MONGODB_URI=mongodb://localhost:27017/pro-jet

# Telegram (опціонально для тестування)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here
```

### 3. Запустіть сервер

```bash
npm start
```

Ви повинні побачити:

```
╔════════════════════════════════════════╗
║     🚀 Pro Jet Backend Server 🚀       ║
╚════════════════════════════════════════╝

  URL: http://localhost:3000
  Environment: development

✅ MongoDB підключена успішно!
✅ Telegram бот ініціалізований
```

**ЯКЩО ОШИБКА:**

```
❌ Помилка підключення MongoDB: connect ECONNREFUSED
```

- MongoDB не запущена
- Перевірте чи MongoDB запущена
- Перевірте MONGODB_URI в .env
- Для MongoDB Atlas: перевірте IP адресу та password

---

## 🧪 КРОК 3: Тестування форми

### Варіант A: Вручну через браузер

**1. Відкрийте сайт**

```
http://localhost:3000
```

Повинна бути головна сторінка.

**2. Перейдіть до Магазину**

Click "Магазин" у меню

**3. Додайте товар**

- Click на товар
- Click "Додати у кошик"
- Повинно з'явитися повідомлення

**4. Перейдіть до Checkout**

- Click "Оформити" або перейдіть:
```
http://localhost:3000/checkout
```

**5. Заповніть форму**

```
Ім'я: Тест Юзер
Прізвище: Тестовий
Email: test@example.com
Телефон: +380 50 123 45 67
Місто: Київ
Адреса: вул. Крещатик, 1
Коментарі: Тестове замовлення
```

**6. Click "Відправити замовлення"**

### Варіант B: Через curl (для програмістів)

```bash
curl -X POST http://localhost:3000/api/orders \
  -H "Content-Type: application/json" \
  -d '{
    "customer": {
      "name": "Іван Петренко",
      "email": "ivan@example.com",
      "phone": "+380 50 123 45 67",
      "city": "Київ",
      "address": "вул. Крещатик, 1"
    },
    "items": [
      {
        "name": "Дерев'яна табличка",
        "quantity": 1,
        "price": 250
      }
    ],
    "service": "shop",
    "pricing": {
      "basePrice": 250,
      "discount": 0,
      "totalPrice": 250,
      "currency": "UAH"
    },
    "notes": "Тестове замовлення"
  }'
```

Повинна бути відповідь:

```json
{
  "success": true,
  "message": "Замовлення успішно створено!",
  "order": {
    "_id": "...",
    "orderNumber": "ORD-...",
    "customer": {...},
    "status": "new",
    ...
  }
}
```

---

## ✅ Перевірка роботи

### 1. Сервер запущено

Повинна бути відповідь:

```bash
curl http://localhost:3000/api/health
```

Відповідь:

```json
{
  "status": "OK",
  "message": "Pro Jet Backend API is running"
}
```

### 2. MongoDB підключена

Дивіться логи:

```
✅ MongoDB підключена успішно!
```

### 3. Замовлення збережено

Зайдіть у MongoDB та перевірте:

**MongoDB Atlas:**
- Dashboard → Collections
- Виберіть базу "pro-jet"
- Виберіть колекцію "orders"
- Повинно бути ваше замовлення

**Локальний MongoDB:**

```bash
mongosh
use pro-jet
db.orders.find()
# Повинна бути ваше замовлення
```

### 4. Telegram отримав (якщо налаштовано)

- Дивіться Telegram чат
- Повинне бути повідомлення про замовлення

---

## 🐛 Рішення проблем

### Проблема 1: "ECONNREFUSED" при підключенні до MongoDB

**Причина:** MongoDB не запущена або неправильна адреса

**Рішення:**

```bash
# Перевірте MongoDB:
# Для Atlas - перевірте пароль та IP
# Для локального - запустіть MongoDB

# Linux/Mac
brew services start mongodb-community
# ИЛИ
sudo systemctl start mongodb

# Потім перезапустіть сервер
npm start
```

### Проблема 2: Форма не відправляється

**Рішення:**

1. Відкрийте Developer Tools (F12)
2. Дивіться Console tab
3. Заповніть форму та натисніть "Відправити"
4. Дивіться помилку в консолі

Типові помилки:

```
POST http://localhost:3000/api/orders 500 Internal Server Error
```

- Перевірте MongoDB підключення
- Дивіться логи сервера (`npm start`)

### Проблема 3: Помилка в логах сервера

```
❌ Помилка створення замовлення: ValidationError
```

- Перевірте що форма заповнена коректно
- Всі поля повинні бути заповнені

---

## 📊 Для Railway розгортання

**ВАЖЛИВО: На Railway все вже налаштовано!**

1. Railway автоматично використовує правильний MONGODB_URI (внутрішньої адреси)
2. Форма буде працювати відразу після розгортання
3. Просто додайте Telegram Bot (дивіться TELEGRAM_SETUP.md)

**Як розгорнути:**

```bash
# 1. Push на GitHub
git add .
git commit -m "Оновлено конфігурацію БД"
git push origin main

# 2. Railway автоматично розгорне

# 3. Перевірте в Railway Dashboard
# Дивіться логи - повинна бути ✅ MongoDB підключена

# 4. Тестуйте на живому сайті
# https://your-railway-url/checkout
```

---

## 🔑 Найважливішого

| Что | Де берати | Як використовувати |
|-----|-----------|------------------|
| **MONGODB_URI** | MongoDB Atlas / Локально | Вставити в .env |
| **TELEGRAM_TOKEN** | @BotFather | Вставити в .env |
| **TELEGRAM_CHAT_ID** | @userinfobot | Вставити в .env |

---

## ✨ Готово!

Коли ви виконаєте всі кроки, ви будете мати:

✅ Запущений Express сервер на порту 3000
✅ Підключену MongoDB (Atlas або локально)
✅ Робочу форму замовлення
✅ Замовлення зберігаються у БД
✅ Telegram сповіщення про замовлення (якщо налаштовано)

---

## 📞 Контроль список

- [ ] MongoDB встановлена та запущена
- [ ] MONGODB_URI додана в .env
- [ ] `npm install` виконана
- [ ] `npm start` запущено без помилок
- [ ] `http://localhost:3000` доступна
- [ ] Форма заповняється та відправляється
- [ ] Замовлення з'являється у MongoDB
- [ ] (Опціонально) Telegram отримує сповіщення

---

**Остання оновлення:** 02.12.2024
**Версія:** 1.0
